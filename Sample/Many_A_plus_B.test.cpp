#define PROBLEM "https://judge.yosupo.jp/problem/many_aplusb"

#include "../template.h"

void solve(){
    int t; cin >> t;
    while(t--) {
        long long a, b; cin >> a >> b;
        cout << a + b << '\n';
    }
}